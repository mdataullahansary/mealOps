$kind: http-request
name: Login
method: POST
url: "{{baseUrl}}/api/v1/auth/login"
order: 2000
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "email": "rid@example.com",
      "password": "securepass"
    }
